---
id: ACC-001
---

# Acceptance Evidence: NiveauIndex Cycle 1

## Request Outcome

`REQ-001`: make `curriculum-store` `NiveauIndex` create/update behavior satisfy
the confirmed derived-data invariant.

## Why This Acceptance Exists

The maintainer accepted Cycle 1 into the `spiral-development` branch after the
human evaluation surface was presented. The cycle remains intended for later
merge into `main` by the maintainer.

## Scenario / Interaction

The maintainer reviewed the cycle summary and stated:

```text
Ok, cycle 1 is accepted into the spiral-development branch, I'll merge with main later.
```

Before this acceptance, `origin/spiral-development` had already integrated the
cycle via merge commit `21448998f50e399a5ab6734e7ceb8696eeca15ed`.

## Expected Observable Outcome

The cycle result is accepted as sufficient for the `spiral-development` branch:

- `NiveauIndex` create/update behavior implements the confirmed invariant;
- focused tests encode the corrected behavior;
- discovered coverage, lint, and system-level integration concerns are carried
  forward rather than blocking this cycle's acceptance.

## Result

- [x] Accepted
- [ ] Rejected
- [ ] Inconclusive

## Intended-Audience Evidence

Explicit maintainer acceptance in the connected Codex conversation on
2026-08-17.

## Limits / Unresolved Questions

- The maintainer will merge `spiral-development` to `main` later.
- No new implementation or system-level repository restructuring was performed
  as part of acceptance.
- Coverage-policy failure, missing ESLint configuration, and system-level Spiral
  integration remain concerns for later cycles.

> Record exact `sd:accepts` and relevant `sd:derivedFrom` references in the
> companion Turtle resource.
